// <auto-generated />
// This file is auto-generated by NFMWorld.LuaSourceGenerator.
// ReSharper disable All
#nullable enable

using LuaNET.LuaJIT;
using static LuaNET.LuaJIT.Lua;

namespace nfm_world_library.Lua;

public partial class LuaBindings
{
    // =========== Bindings for Fixed64 (Fixed64) ===========
    private static void Register_Fixed64(lua_State L)
    {
        RegisterMetatable<FixedMathSharp.Fixed64>("MT_Fixed64");

        // Create metatable for instances
        luaL_newmetatable(L, "MT_Fixed64");

        // __gc metamethod
        lua_pushcfunction(L, (Fixed64__gc));
        lua_setfield(L, -2, "__gc");

        // __index metamethod
        lua_pushcfunction(L, (Fixed64__index));
        lua_setfield(L, -2, "__index");

        // __newindex metamethod
        lua_pushcfunction(L, (Fixed64__newindex));
        lua_setfield(L, -2, "__newindex");

        // Operator: __add
        lua_pushcfunction(L, (Fixed64_op_op_Addition));
        lua_setfield(L, -2, "__add");

        // Operator: __add
        lua_pushcfunction(L, (Fixed64_op_op_Addition));
        lua_setfield(L, -2, "__add");

        // Operator: __add
        lua_pushcfunction(L, (Fixed64_op_op_Addition));
        lua_setfield(L, -2, "__add");

        // Operator: __add
        lua_pushcfunction(L, (Fixed64_op_op_Addition));
        lua_setfield(L, -2, "__add");

        // Operator: __add
        lua_pushcfunction(L, (Fixed64_op_op_Addition));
        lua_setfield(L, -2, "__add");

        // Operator: __sub
        lua_pushcfunction(L, (Fixed64_op_op_Subtraction));
        lua_setfield(L, -2, "__sub");

        // Operator: __sub
        lua_pushcfunction(L, (Fixed64_op_op_Subtraction));
        lua_setfield(L, -2, "__sub");

        // Operator: __sub
        lua_pushcfunction(L, (Fixed64_op_op_Subtraction));
        lua_setfield(L, -2, "__sub");

        // Operator: __sub
        lua_pushcfunction(L, (Fixed64_op_op_Subtraction));
        lua_setfield(L, -2, "__sub");

        // Operator: __sub
        lua_pushcfunction(L, (Fixed64_op_op_Subtraction));
        lua_setfield(L, -2, "__sub");

        // Operator: __mul
        lua_pushcfunction(L, (Fixed64_op_op_Multiply));
        lua_setfield(L, -2, "__mul");

        // Operator: __mul
        lua_pushcfunction(L, (Fixed64_op_op_Multiply));
        lua_setfield(L, -2, "__mul");

        // Operator: __mul
        lua_pushcfunction(L, (Fixed64_op_op_Multiply));
        lua_setfield(L, -2, "__mul");

        // Operator: __div
        lua_pushcfunction(L, (Fixed64_op_op_Division));
        lua_setfield(L, -2, "__div");

        // Operator: __div
        lua_pushcfunction(L, (Fixed64_op_op_Division));
        lua_setfield(L, -2, "__div");

        // Operator: __mod
        lua_pushcfunction(L, (Fixed64_op_op_Modulus));
        lua_setfield(L, -2, "__mod");

        // Operator: __unm
        lua_pushcfunction(L, (Fixed64_op_op_UnaryNegation));
        lua_setfield(L, -2, "__unm");

        // Operator: __gt
        lua_pushcfunction(L, (Fixed64_op_op_GreaterThan));
        lua_setfield(L, -2, "__gt");

        // Operator: __lt
        lua_pushcfunction(L, (Fixed64_op_op_LessThan));
        lua_setfield(L, -2, "__lt");

        // Operator: __ge
        lua_pushcfunction(L, (Fixed64_op_op_GreaterThanOrEqual));
        lua_setfield(L, -2, "__ge");

        // Operator: __le
        lua_pushcfunction(L, (Fixed64_op_op_LessThanOrEqual));
        lua_setfield(L, -2, "__le");

        // Operator: __eq
        lua_pushcfunction(L, (Fixed64_op_op_Equality));
        lua_setfield(L, -2, "__eq");

        // __tostring metamethod
        lua_pushcfunction(L, (Fixed64__tostring));
        lua_setfield(L, -2, "__tostring");

        lua_pop(L, 1);

        // Create type table for Fixed64
        lua_newtable(L);

        // Constructor: new()
        lua_pushcfunction(L, (Fixed64_new));
        lua_setfield(L, -2, "new");

        // Static method: fraction
        lua_pushcfunction(L, (Fixed64_static_fraction));
        lua_setfield(L, -2, "fraction");

        // Static method: sign
        lua_pushcfunction(L, (Fixed64_static_sign));
        lua_setfield(L, -2, "sign");

        // Static method: isInteger
        lua_pushcfunction(L, (Fixed64_static_isInteger));
        lua_setfield(L, -2, "isInteger");

        // Static method: parse
        lua_pushcfunction(L, (Fixed64_static_parse));
        lua_setfield(L, -2, "parse");

        // Static method: fromRaw
        lua_pushcfunction(L, (Fixed64_static_fromRaw));
        lua_setfield(L, -2, "fromRaw");

        // Static method: toDouble
        lua_pushcfunction(L, (Fixed64_static_toDouble));
        lua_setfield(L, -2, "toDouble");

        // Static method: toFloat
        lua_pushcfunction(L, (Fixed64_static_toFloat));
        lua_setfield(L, -2, "toFloat");

        // Static method: toDecimal
        lua_pushcfunction(L, (Fixed64_static_toDecimal));
        lua_setfield(L, -2, "toDecimal");

        lua_setglobal(L, "Fixed64");
    }

    private static int Fixed64__gc(lua_State L)
    {
        var ptr = lua_touserdata(L, 1);
        if (ptr != 0)
        {
            unsafe
            {
                var id = *(int*)ptr;
                RemoveObject<FixedMathSharp.Fixed64>(id);
            }
        }
        return 0;
    }

    private static int Fixed64__index(lua_State L)
    {
        var obj = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        var key = lua_tostring(L, 2);
        if (key == null) { lua_pushnil(L); return 1; }

        switch (key)
        {
            case "m_rawValue":
                PushValue(L, ((FixedMathSharp.Fixed64)obj).m_rawValue);
                return 1;
            case "offset":
                lua_pushcfunction(L, (Fixed64_method_offset));
                return 1;
            case "rawToString":
                lua_pushcfunction(L, (Fixed64_method_rawToString));
                return 1;
            case "toString":
                lua_pushcfunction(L, (Fixed64_method_toString));
                return 1;
            case "equals":
                lua_pushcfunction(L, (Fixed64_method_equals));
                return 1;
            case "getHashCode":
                lua_pushcfunction(L, (Fixed64_method_getHashCode));
                return 1;
            case "compareTo":
                lua_pushcfunction(L, (Fixed64_method_compareTo));
                return 1;
            case "getType":
                lua_pushcfunction(L, (Fixed64_method_getType));
                return 1;
            default:
                lua_pushnil(L);
                return 1;
        }
    }

    private static int Fixed64__newindex(lua_State L)
    {
        var obj = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        var key = lua_tostring(L, 2);
        if (key == null) return 0;

        switch (key)
        {
            case "m_rawValue":
                try
                {
                    obj.m_rawValue = ToObject<long>(L, 3)!;
                }
                catch (System.Exception ex)
                {
                    luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                    return 0;
                }
                UpdateStruct(L, 1, obj);
                break;
        }
        return 0;
    }

    private static int Fixed64__tostring(lua_State L)
    {
        var obj = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);
        lua_pushstring(L, obj.ToString() ?? "");
        return 1;
    }

    private static int Fixed64_op_op_Addition(lua_State L)
    {
        // Multiple operator overloads - find best match
        int bestScore = -1;
        int bestIndex = -1;

        // Try overload 0: op_Addition(FixedMathSharp.Fixed64, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next0;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next0;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 0;
            }
        }
        next0:

        // Try overload 1: op_Addition(FixedMathSharp.Fixed64, int)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next1;
            else score += score0;
            int score1 = ScoreParameterCompatibility<int>(L, 2);
            if (score1 < 0) goto next1;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 1;
            }
        }
        next1:

        // Try overload 2: op_Addition(int, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<int>(L, 1);
            if (score0 < 0) goto next2;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next2;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 2;
            }
        }
        next2:

        // Try overload 3: op_Addition(FixedMathSharp.Fixed64, float)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next3;
            else score += score0;
            int score1 = ScoreParameterCompatibility<float>(L, 2);
            if (score1 < 0) goto next3;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 3;
            }
        }
        next3:

        // Try overload 4: op_Addition(float, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<float>(L, 1);
            if (score0 < 0) goto next4;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next4;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 4;
            }
        }
        next4:

        switch (bestIndex)
        {
            case 0:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left + right;
                    PushValue(L, result);
                    return 1;
                }
            case 1:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<int>(L, 2)!;
                    var result = left + right;
                    PushValue(L, result);
                    return 1;
                }
            case 2:
                {
                    var left = ToObject<int>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left + right;
                    PushValue(L, result);
                    return 1;
                }
            case 3:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<float>(L, 2)!;
                    var result = left + right;
                    PushValue(L, result);
                    return 1;
                }
            case 4:
                {
                    var left = ToObject<float>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left + right;
                    PushValue(L, result);
                    return 1;
                }
            default:
                luaL_error(L, "No compatible operator overload found");
                return 0;
        }
    }

    private static int Fixed64_op_op_Subtraction(lua_State L)
    {
        // Multiple operator overloads - find best match
        int bestScore = -1;
        int bestIndex = -1;

        // Try overload 0: op_Subtraction(FixedMathSharp.Fixed64, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next0;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next0;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 0;
            }
        }
        next0:

        // Try overload 1: op_Subtraction(FixedMathSharp.Fixed64, int)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next1;
            else score += score0;
            int score1 = ScoreParameterCompatibility<int>(L, 2);
            if (score1 < 0) goto next1;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 1;
            }
        }
        next1:

        // Try overload 2: op_Subtraction(int, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<int>(L, 1);
            if (score0 < 0) goto next2;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next2;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 2;
            }
        }
        next2:

        // Try overload 3: op_Subtraction(FixedMathSharp.Fixed64, float)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next3;
            else score += score0;
            int score1 = ScoreParameterCompatibility<float>(L, 2);
            if (score1 < 0) goto next3;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 3;
            }
        }
        next3:

        // Try overload 4: op_Subtraction(float, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<float>(L, 1);
            if (score0 < 0) goto next4;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next4;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 4;
            }
        }
        next4:

        switch (bestIndex)
        {
            case 0:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left - right;
                    PushValue(L, result);
                    return 1;
                }
            case 1:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<int>(L, 2)!;
                    var result = left - right;
                    PushValue(L, result);
                    return 1;
                }
            case 2:
                {
                    var left = ToObject<int>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left - right;
                    PushValue(L, result);
                    return 1;
                }
            case 3:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<float>(L, 2)!;
                    var result = left - right;
                    PushValue(L, result);
                    return 1;
                }
            case 4:
                {
                    var left = ToObject<float>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left - right;
                    PushValue(L, result);
                    return 1;
                }
            default:
                luaL_error(L, "No compatible operator overload found");
                return 0;
        }
    }

    private static int Fixed64_op_op_Multiply(lua_State L)
    {
        // Multiple operator overloads - find best match
        int bestScore = -1;
        int bestIndex = -1;

        // Try overload 0: op_Multiply(FixedMathSharp.Fixed64, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next0;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next0;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 0;
            }
        }
        next0:

        // Try overload 1: op_Multiply(FixedMathSharp.Fixed64, int)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next1;
            else score += score0;
            int score1 = ScoreParameterCompatibility<int>(L, 2);
            if (score1 < 0) goto next1;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 1;
            }
        }
        next1:

        // Try overload 2: op_Multiply(int, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<int>(L, 1);
            if (score0 < 0) goto next2;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next2;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 2;
            }
        }
        next2:

        switch (bestIndex)
        {
            case 0:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left * right;
                    PushValue(L, result);
                    return 1;
                }
            case 1:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<int>(L, 2)!;
                    var result = left * right;
                    PushValue(L, result);
                    return 1;
                }
            case 2:
                {
                    var left = ToObject<int>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left * right;
                    PushValue(L, result);
                    return 1;
                }
            default:
                luaL_error(L, "No compatible operator overload found");
                return 0;
        }
    }

    private static int Fixed64_op_op_Division(lua_State L)
    {
        // Multiple operator overloads - find best match
        int bestScore = -1;
        int bestIndex = -1;

        // Try overload 0: op_Division(FixedMathSharp.Fixed64, FixedMathSharp.Fixed64)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next0;
            else score += score0;
            int score1 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 2);
            if (score1 < 0) goto next0;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 0;
            }
        }
        next0:

        // Try overload 1: op_Division(FixedMathSharp.Fixed64, int)
        {
            int score = 0;
            int score0 = ScoreParameterCompatibility<FixedMathSharp.Fixed64>(L, 1);
            if (score0 < 0) goto next1;
            else score += score0;
            int score1 = ScoreParameterCompatibility<int>(L, 2);
            if (score1 < 0) goto next1;
            else score += score1;
            if (score > bestScore)
            {
                bestScore = score;
                bestIndex = 1;
            }
        }
        next1:

        switch (bestIndex)
        {
            case 0:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
                    var result = left / right;
                    PushValue(L, result);
                    return 1;
                }
            case 1:
                {
                    var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
                    var right = ToObject<int>(L, 2)!;
                    var result = left / right;
                    PushValue(L, result);
                    return 1;
                }
            default:
                luaL_error(L, "No compatible operator overload found");
                return 0;
        }
    }

    private static int Fixed64_op_op_Modulus(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        var result = left % right;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_UnaryNegation(lua_State L)
    {
        var operand = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var result = -operand;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_GreaterThan(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        var result = left > right;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_LessThan(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        var result = left < right;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_GreaterThanOrEqual(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        var result = left >= right;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_LessThanOrEqual(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        var result = left <= right;
        PushValue(L, result);
        return 1;
    }

    private static int Fixed64_op_op_Equality(lua_State L)
    {
        var left = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
        var right = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
        lua_pushboolean(L, left == right ? 1 : 0);
        return 1;
    }

    private static int Fixed64_new(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 0)
        {
            var obj = new FixedMathSharp.Fixed64();
            PushObject(L, obj, "MT_Fixed64");
            return 1;
        }

        if (argCount == 1)
        {
            // Multiple constructors with same argument count - find best match
            int bestScore = -1;
            int bestIndex = -1;

            // Try constructor 0: new Fixed64(int)
            {
                int score = 0;
                int score0 = ScoreParameterCompatibility<int>(L, 1);
                if (score0 < 0) goto next0;
                else score += score0;
                if (score > bestScore)
                {
                    bestScore = score;
                    bestIndex = 0;
                }
            }
            next0:

            // Try constructor 1: new Fixed64(double)
            {
                int score = 0;
                int score0 = ScoreParameterCompatibility<double>(L, 1);
                if (score0 < 0) goto next1;
                else score += score0;
                if (score > bestScore)
                {
                    bestScore = score;
                    bestIndex = 1;
                }
            }
            next1:

            switch (bestIndex)
            {
                case 0:
                    {
                        var arg0 = ToObject<int>(L, 1)!;
                        try
                        {
                            var obj = new FixedMathSharp.Fixed64(arg0);
                            PushObject(L, obj, "MT_Fixed64");
                            return 1;
                        }
                        catch (System.Exception ex)
                        {
                            luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                            return 0;
                        }
                    }
                case 1:
                    {
                        var arg0 = ToObject<double>(L, 1)!;
                        try
                        {
                            var obj = new FixedMathSharp.Fixed64(arg0);
                            PushObject(L, obj, "MT_Fixed64");
                            return 1;
                        }
                        catch (System.Exception ex)
                        {
                            luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                            return 0;
                        }
                    }
                default:
                    luaL_error(L, "No compatible constructor found for Fixed64");
                    return 0;
            }
        }

        luaL_error(L, "Invalid arguments for Fixed64 constructor");
        return 0;
    }

    private static int Fixed64_static_fraction(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 2)
        {
            var arg0 = ToObject<double>(L, 1)!;
            var arg1 = ToObject<double>(L, 2)!;
            try
            {
                var result = FixedMathSharp.Fixed64.Fraction(arg0, arg1);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for fraction");
        return 0;
    }

    private static int Fixed64_static_sign(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.Sign(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for sign");
        return 0;
    }

    private static int Fixed64_static_isInteger(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<FixedMathSharp.Fixed64>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.IsInteger(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for isInteger");
        return 0;
    }

    private static int Fixed64_static_parse(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<string>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.Parse(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for parse");
        return 0;
    }

    private static int Fixed64_static_fromRaw(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<long>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.FromRaw(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for fromRaw");
        return 0;
    }

    private static int Fixed64_static_toDouble(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<long>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.ToDouble(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for toDouble");
        return 0;
    }

    private static int Fixed64_static_toFloat(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<long>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.ToFloat(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for toFloat");
        return 0;
    }

    private static int Fixed64_static_toDecimal(lua_State L)
    {
        var argCount = lua_gettop(L);

        if (argCount == 1)
        {
            var arg0 = ToObject<long>(L, 1)!;
            try
            {
                var result = FixedMathSharp.Fixed64.ToDecimal(arg0);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for toDecimal");
        return 0;
    }

    private static int Fixed64_method_offset(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 1)
        {
            var arg0 = ToObject<int>(L, 2)!;
            try
            {
                ((FixedMathSharp.Fixed64)self).Offset(arg0);
                UpdateStruct(L, 1, self);
                return 0;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for offset");
        return 0;
    }

    private static int Fixed64_method_rawToString(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 0)
        {
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).RawToString();
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for rawToString");
        return 0;
    }

    private static int Fixed64_method_toString(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 0)
        {
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).ToString();
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for toString");
        return 0;
    }

    private static int Fixed64_method_equals(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 1)
        {
            object? arg0;
            if (lua_isnil(L, 2) != 0)
                arg0 = null;
            else
                arg0 = ToObject<object>(L, 2)!;
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).Equals(arg0);
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for equals");
        return 0;
    }

    private static int Fixed64_method_getHashCode(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 0)
        {
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).GetHashCode();
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for getHashCode");
        return 0;
    }

    private static int Fixed64_method_compareTo(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 1)
        {
            var arg0 = ToObject<FixedMathSharp.Fixed64>(L, 2)!;
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).CompareTo(arg0);
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for compareTo");
        return 0;
    }

    private static int Fixed64_method_getType(lua_State L)
    {
        var argCount = lua_gettop(L) - 1; // First arg is self

        var self = GetStructFromStack<FixedMathSharp.Fixed64>(L, 1);

        if (argCount == 0)
        {
            try
            {
                var result = ((FixedMathSharp.Fixed64)self).GetType();
                UpdateStruct(L, 1, self);
                PushValue(L, result);
                return 1;
            }
            catch (System.Exception ex)
            {
                luaL_error(L, $"{ex.GetType().Name}: {ex.Message}\n{ex.StackTrace}");
                return 0;
            }
        }

        luaL_error(L, "Invalid arguments for getType");
        return 0;
    }

}
